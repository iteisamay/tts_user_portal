export const getMobileMenuStyles = (darkMode) => ({
    Cardbg: darkMode
        ? "bg-gray-700"
        : "bg-white ",

    Headerbg: darkMode
        ? "bg-gray-700"
        : "bg-white",

    Playlistbg: darkMode
        ? "bg-gray-700"
        : "bg-white",

    Playlisticons: darkMode
        ? "text-white"
        : "text-gray-500",
});