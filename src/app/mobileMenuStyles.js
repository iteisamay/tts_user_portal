export const getMobileMenuStyles = (darkMode) => ({
    Cardbg: darkMode
        ? "bg-gray-700"
        : "bg-cyan-100 ",
        
    Headerbg: darkMode
        ? "bg-gray-700"
        : "bg-cyan-100 ",

    Playlistbg: darkMode
        ? "bg-gray-700"
        : "bg-cyan-100 ",

    Playlisticons: darkMode
        ? "text-white"
        : "text-gray-500",

    //ThemeToggle.js
    feedback: darkMode
        ? "bg-gray-700 text-white"
        : "bg-white text-black",
    
});